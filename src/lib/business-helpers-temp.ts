/**
 * Helpers métier pour Edgemy - VERSION TEMPORAIRE
 * Ces fonctions seront activées après la migration de la base de données
 * Pour l'instant, elles sont commentées pour éviter les erreurs de build
 */

// import { db } from '@/lib/db';
// import { RoleType, SubscriptionStatus, PlanType } from '@prisma/client';

// Les fonctions seront décommentées après la migration Prisma en production
// Voir BUSINESS_LOGIC.md pour la documentation complète

export const TEMP_NOTE = `
Les helpers métier sont temporairement désactivés.
Ils seront activés après la migration de la base de données en production.
`;
